# 使用项目本地虚拟环境 .venv
export VIRTUAL_ENV="$PWD/.venv"

# 把 .venv/bin 放在 PATH 最前面（direnv 自带 PATH_add）
PATH_add "$VIRTUAL_ENV/bin"

# 让 ansible 明确使用这个 venv 里的 python
export ANSIBLE_PYTHON_INTERPRETER="$VIRTUAL_ENV/bin/python"
