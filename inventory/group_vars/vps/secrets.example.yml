---
# 仅示例：请勿直接引用，复制内容后使用 `ansible-vault encrypt` 生成真正的 secrets.yml。
# secrets.yml 必须包含 Cloudflared Tunnel 的凭据 JSON（由 `cloudflared tunnel create` 生成，内部自带 TunnelID）。
cloudflared_tunnel_credentials_json: |
  {"AccountTag":"00000000000000000000000000000000","TunnelSecret":"base64-encoded-secret","TunnelID":"00000000-0000-0000-0000-000000000000"}
